import { DefaultTheme } from 'styled-components';

const light = {
  primary: '#ffffff',
  text: '#18191a',
};

const dark = {
  primary: '#18191a',
  text: '#ffffff',
};

const theme: DefaultTheme = {
  light,
  dark,
  button: {
    fontFamily: 'cursive',
  },
};

export default theme;
